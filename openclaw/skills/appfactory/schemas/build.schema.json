{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AppFactory Build Output",
  "description": "Schema for the output of the Builder agent after scaffolding a project",
  "type": "object",
  "required": ["repo_name", "repo_url", "files_created"],
  "properties": {
    "repo_name": {
      "type": "string",
      "description": "Kebab-case repository name"
    },
    "repo_url": {
      "type": "string",
      "description": "Full GitHub URL of the created repository"
    },
    "files_created": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "List of file paths created in the scaffold"
    }
  },
  "additionalProperties": false
}
